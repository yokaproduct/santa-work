namespace Santa.UI
{
    /// <summary>
    /// テストプレイ用の仮置き画面(Screen_Title / Screen_ModeSelect)が使う最小のRefs。
    /// 追加のフィールドを持たない(共通3フィールドだけで足りるため)。
    /// ★ディレクターの指示により「UIや画像は作成せず単純なobjectを入れるだけ」の段階。
    /// </summary>
    public class PlaceholderScreenRefs : ScreenRefsBase
    {
    }
}
